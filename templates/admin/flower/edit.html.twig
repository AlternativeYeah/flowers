{% extends 'admin/starter.html.twig' %}
{% block content %}
        <section class="content">
            <div class="row">
                <div class="col-md-6">
                    <!-- general form elements -->
                    <div class="box box-primary">
                        <div class="box-header with-border">
                            <h3 class="box-title">Редактировать товар</h3>
                        </div>
                        <img width="300px" src="{{ asset('uploads/img/flower/' ~ img) }}">
                        <!-- /.box-header -->
                        <!-- form start -->

                        {{ form_start(form) }}
                        {{ form_widget(form) }}
                        {{ form_end(form) }}
                    </div>
                    <!-- /.box -->
                </div>
            </div>
        </section>
{% endblock %}